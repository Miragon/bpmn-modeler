pluginGroup = io.miragon.bpmn
pluginVersion = 0.0.1

# 233 = IntelliJ IDEA 2023.3 (first release with stable JCEF + modern FileEditorProvider).
pluginSinceBuild = 233
# No upper bound: the APIs we use (FileEditorProvider + JBCefBrowser) have been
# stable since 2023.3. Bump this only if a future IDE release breaks the plugin.
pluginUntilBuild =

platformType = IC
platformVersion = 2023.3.6

kotlin.stdlib.default.dependency = false
org.gradle.caching = true
org.gradle.jvmargs = -Xmx2g -XX:+UseParallelGC
