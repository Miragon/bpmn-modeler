appId: io.miragon.bpmn-modeler
productName: Miragon BPMN Modeler
copyright: Copyright (c) Miragon
electronVersion: 42.3.0
directories:
  buildResources: resources
  output: dist
publish:
  provider: generic
  url: https://github.com/Miragon/bpmn-modeler/releases/download/standalone-latest
fileAssociations:
  - ext: bpmn
    name: BPMN Process Model
    description: BPMN 2.0 Process Diagram
    mimeType: application/bpmn+xml
    role: Editor
  - ext: dmn
    name: DMN Decision Model
    description: DMN 1.3 Decision Table
    mimeType: application/dmn+xml
    role: Editor

files:
  - lib/**/*
  - src-gen/**/*
  - scripts/**/*
  - resources/splash.html
  - resources/miragon-logo.png
  - package.json
  # exclude sources & declarations
  - "!**/*.ts"
  - "!**/*.d.ts"
  - "!**/*.ts.map"
  - "!**/*.js.map"
  - "!**/*.map"
  - "!**/*.map.gz"
  - "!**/*.js.gz"
  - "!**/*.css.gz"
  # exclude docs & changelogs that ride along in node_modules
  - "!**/*.md"
  - "!**/*.markdown"
  - "!**/{CHANGELOG,HISTORY,RELEASES,README,NEWS}*"
  - "!**/LICENSE*"
  - "!**/AUTHORS*"
  # exclude dev-only dirs
  - "!**/{test,tests,__tests__,__mocks__,example,examples,doc,docs,fixture,fixtures,spec,specs}/**"
  - "!**/{.github,.vscode,.idea,.husky}/**"
  # exclude config / metadata files
  - "!**/{.eslintrc,.prettierrc,.editorconfig,.nycrc,.babelrc,.jshintrc}*"
  - "!**/*.{yml,yaml}"
  - "!**/tsconfig*.json"
  - "!**/vitest.config*"
  - "!**/.travis.yml"
extraResources:
  - from: plugins
    to: app/plugins
    filter:
      - "**/*"
mac:
  category: public.app-category.developer-tools
  target:
    - dmg
  icon: resources/icon.png
  identity: "Miragon GmbH (G5JZQ328LJ)"
  hardenedRuntime: true
  notarize: true
dmg:
  format: ULFO
  # GitHub Releases rewrites spaces in uploaded asset names to dots.
  artifactName: Miragon.BPMN.Modeler-${version}-${arch}.${ext}
win:
  target:
    - nsis
  icon: resources/icon.ico
  # Mirror the DMG naming; GitHub Releases rewrites spaces in uploaded asset
  # names to dots, so we pre-flatten to keep download URLs stable.
  artifactName: Miragon.BPMN.Modeler-${version}-${arch}.${ext}
linux:
  target:
    - dir
  icon: resources/icon.png
  executableName: miragon-bpmn-modeler
  category: Development
