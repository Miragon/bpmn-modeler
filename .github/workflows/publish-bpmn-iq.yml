name: Publish BPMN IQ

# Builds and publishes the bpmn-iq VS Code extension when a GitHub Release
# with a bpmn-iq-v* tag is published. Also supports a manual workflow_dispatch
# with a dry-run option that produces a vsix artifact without publishing.

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      dry-run:
        description: 'Dry run (build + package, do not publish)'
        required: false
        type: boolean
        default: true

permissions:
  contents: write

concurrency:
  group: bpmn-iq-publish
  cancel-in-progress: false

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'release' && startsWith(github.event.release.tag_name, 'bpmn-iq-v'))

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Check pinned dependencies
        uses: Miragon/pin-npm-dependencies@v1

      - name: Corepack enable
        run: corepack enable

      - name: Setup Node 20
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: yarn

      - name: Install dependencies (focused)
        run: yarn workspaces focus bpmn-modeler bpmn-iq-plugin vs-code-bpmn-modeler @miragon/bpmn-modeler-webview @miragon/dmn-modeler-webview @miragon/bpmn-modeler-deployment-webview @miragon/bpmn-modeler-shared @miragon/bpmn-modeler-append-menu @miragon/bpmn-modeler-clipboard @miragon/bpmn-modeler-i18n @miragon/bpmn-modeler-element-template-chooser @miragon/create-append-c7-element-templates

      - name: Resolve version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            VERSION="${{ github.event.release.tag_name }}"
            VERSION="${VERSION#bpmn-iq-v}"
          else
            VERSION=$(node -e "console.log(require('./apps/bpmn-iq-plugin/package.json').version)")
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Verify package.json version matches tag
        if: github.event_name == 'release'
        run: |
          PKG_VERSION=$(node -e "console.log(require('./apps/bpmn-iq-plugin/package.json').version)")
          if [ "$PKG_VERSION" != "${{ steps.version.outputs.version }}" ]; then
            echo "package.json version ($PKG_VERSION) does not match release tag (${{ steps.version.outputs.version }})." && exit 1
          fi

      - name: Build
        run: yarn build
        env:
          MIRAGON_CLOUD_DAEMON_URL: ${{ secrets.MIRAGON_CLOUD_DAEMON_URL }}

      - name: Install vsce
        run: npm install -g @vscode/vsce@3.2.2

      - name: Package vsix
        working-directory: dist/apps/bpmn-iq-plugin
        run: vsce package --out bpmn-iq-plugin.vsix --yarn --no-dependencies

      - name: Upload vsix artifact (dry-run)
        if: github.event_name == 'workflow_dispatch' && inputs.dry-run == true
        uses: actions/upload-artifact@v7
        with:
          name: bpmn-iq-plugin-v${{ steps.version.outputs.version }}
          path: dist/apps/bpmn-iq-plugin/bpmn-iq-plugin.vsix
          retention-days: 7

      - name: Attach vsix to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event.release.tag_name }}
          files: dist/apps/bpmn-iq-plugin/bpmn-iq-plugin.vsix

      - name: Publish to VS Code Marketplace
        if: github.event_name == 'release' || (github.event_name == 'workflow_dispatch' && inputs.dry-run == false)
        working-directory: dist/apps/bpmn-iq-plugin
        run: vsce publish --yarn --no-dependencies
        env:
          VSCE_PAT: ${{ secrets.VSCE_PUBLISH }}
